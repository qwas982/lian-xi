#include <stdio.h>

#include "字典.h"

int main(void){
	字典 *测试对象1;
	字典 *测试对象2;
	int 值 = 28;
	测试对象1 = 创建字典();
	测试对象2 = 创建字典();
	加项目标签(测试对象1,"年龄",&值);
	加项目标签(测试对象2,"名字","查尔斯蒂娜");
	printf("我的年龄是: %d \n",*((int *)取元素标签(测试对象1,"年龄")));
	printf("我的名字是: %s \n",取元素标签(测试对象2,"名字"));

	if(!加项目标签(测试对象1,0,&值)){
		printf("我的年龄在索引 %d 是 %d \n",0,*((int *)取元素索引(测试对象1,0)));
	}
	摧毁(测试对象1);
	摧毁(测试对象2);
	return 0;
}