#include <stdlib.h>
#include <stdio.h>

#include "动态数组.h"

int main(){
	动态数组t *动数 = 初始化动态数组();
	for(int 甲 = 1;甲 <= 50;甲++){
		添加(动数,&甲);
	}
	删除(动数,10);
	int 值 = 1000;
	投掷(动数,&值,0);
	值 = 5000;
	int 其它值 = 7000;
	添加(动数,&其它值);
	for(int 甲 = 0; 甲 < 动数 ->大小;甲++){
		printf("值: %d \n",*(int *)取(动数,甲));
	}
	int 无效索引的值 = 10000;
	投掷(动数,&无效索引的值,150);
	return 0;
}